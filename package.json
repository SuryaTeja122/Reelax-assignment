{
  "name": "reelax-assignment",
  "homepage": "https://github.com/SuryaTeja122/Reelax-assignment.git",
  "version": "1.0.0",
  "private": true,
  "dependencies": { ... },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  }
}
